import { SchedulePage } from "@/components/pages/SchedulePage";

export const metadata = {
  title: "Book a Strategy Call | Avrixo",
  description:
    "Book a free 30-minute strategy call with Avrixo to scope your AI or SaaS product — no commitment, no generic demo.",
};

export default function Schedule() {
  return <SchedulePage />;
}
